#!/usr/env python3
import sys
import ..fs2012_flow_sensor.live_plot
if len(sys.argv) > 1:
    port = sys.argv[1]
else:
    port = '/dev/ttyUSB0'
liveplot = live_plot.LivePlot(port=port)
liveplot.run()
